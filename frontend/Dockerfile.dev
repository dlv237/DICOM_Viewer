FROM node:20-slim

WORKDIR /app

# Mount local code in dev via compose override; do basic deps install once
COPY package.json tsconfig.json vite.config.ts index.html ./
COPY src ./src

RUN npm install

EXPOSE 5173
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "5173"]
